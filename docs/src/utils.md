# `src/utils.py`

> *Auto-generated by ai-doc-gen*

---

**utils.py**
================

**Purpose**
--------

This module provides cross-cutting concerns for logging, decorators, and timing.

**Key Components**
------------------

### Logger

*   `setup_logger(name: str = "doc_gen", verbose: bool = False) -> logging.Logger`: Sets up a logger with the given name and verbosity level.
*   `logger`: A pre-configured logger instance.

### Decorators

*   `timer(fn: F) -> F`: A decorator that logs the execution time of a function.
*   `log_step(label: str) -> Callable[[F], F]`: A decorator factory that wraps a function with step start/end log lines.

### Progress Helper

*   `ProgressBar(total: int, label: str = "Progress", width: int = 30)`: A lightweight terminal progress bar class.

**Dependencies**
----------------

*   `time`: For timing-related functionality.
*   `logging`: For logging functionality.
*   `functools`: For decorator functionality.
*   `typing`: For type hinting.

**Usage Example**
-----------------

```python
import utils

# Set up a logger
logger = utils.setup_logger(verbose=True)

# Use the timer decorator
@utils.timer
def my_function():
    # Simulate some work
    time.sleep(1)

# Use the log_step decorator factory
@utils.log_step("My Step")
def my_step_function():
    # Simulate some work
    time.sleep(1)

# Use the progress bar
bar = utils.ProgressBar(total=10, label="Processing")
for i in range(10):
    # Simulate some work
    time.sleep(0.1)
    bar.step()
bar.done()
```

This example demonstrates how to use the logger, timer decorator, log_step decorator factory, and progress bar.