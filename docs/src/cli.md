# `src/cli.py`

> *Auto-generated by ai-doc-gen*

---

**cli.py — Argument Parsing**
================================

**Purpose**
--------

This module is responsible for parsing command-line arguments for the AI Documentation Generator.

**Key Components**
-----------------

* `parse_args()`: Function that returns an `argparse.Namespace` object containing parsed arguments.
* `_DEFAULTS`: Immutable dictionary containing default values for various arguments.

**Dependencies**
-------------

* `argparse`: Module for parsing command-line arguments.

**Usage Example**
----------------

```python
import cli

if __name__ == "__main__":
    args = cli.parse_args()
    print(args)
```

This will print the parsed arguments to the console.

**Argument Parsing**
------------------

The `parse_args()` function uses `argparse` to define the following arguments:

* `root`: Root directory of the project to document (default: current directory).
* `output`: Output docs folder inside the analyzed project (default: `docs`).
* `model`: Ollama model name (default: `llama3.1`).
* `ignore-file`: Path to a text file with extra ignore patterns (one per line).
* `max-file-chars`: Max characters read per source file (default: `6,000`).
* `verbose`: Enable DEBUG-level logging (default: `False`).

The `argparse` module is used to define the argument parser, which is then used to parse the command-line arguments. The parsed arguments are returned as an `argparse.Namespace` object.