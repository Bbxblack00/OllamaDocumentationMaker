# `src/llm.py`

> *Auto-generated by ai-doc-gen*

---

**llm.py — Ollama Inference Module**
=====================================

**Purpose**
--------

This module provides functions for Ollama inference, including environment setup, model pulling, and generation with streaming.

**Key Components**
------------------

### Environment Setup

*   `ensure_ollama()`: Verifies Ollama installation and auto-installs on Linux.
*   `ensure_model(model: str)`: Pulls the requested model (no-op if already present locally).

### Private Streaming Helper

*   `_stream(prompt: str, model: str, label: str = "Generating")`: Sends a prompt to Ollama and streams the response token-by-token, printing live progress to the terminal.

### Generation Functions

*   `generate_readme(context: str, tree: str, doc_refs: list[str], model: str)`: Generates the main README.md for the project.
*   `generate_module_doc(file_path: Path, content: str, model: str)`: Generates technical documentation for a single source file.

**Dependencies**
--------------

*   `ollama`: Ollama library for LLM inference.
*   `src.utils`: Utility functions for logging, timing, and logging steps.

**Usage Example**
-----------------

```python
from llm import generate_readme, generate_module_doc

# Generate README.md
context = "..."  # aggregated source code context
tree = "..."  # ASCII directory tree string
doc_refs = ["module1", "module2"]  # list of relative paths to per-module doc files
model = "ollama_model_name"
readme = generate_readme(context, tree, doc_refs, model)

# Generate module documentation
file_path = Path("path/to/module.py")
content = "..."  # source code content
module_doc = generate_module_doc(file_path, content, model)
```

Note: This documentation is generated using the `generate_module_doc` function itself.