# `main.py`

> *Auto-generated by ai-doc-gen*

---

**main.py**
================

**Purpose**
--------

This module orchestrates the AI Doc Generator, responsible for generating documentation for a project.

**Key Components**
------------------

*   **`parse_args`**: Parses command-line arguments.
*   **`ensure_ollama`** and **`ensure_model`**: Verify Ollama and model availability.
*   **`load_ignore_patterns`**: Load ignore patterns from a file.
*   **`iter_source_files`**: Scan source files (generator).
*   **`build_tree`**: Build a project tree (cross-platform).
*   **`aggregate_context`**: Aggregate source context.
*   **`generate_module_doc`**: Generate per-module documentation.
*   **`write_module_doc`**: Write per-module documentation to file.
*   **`generate_readme`**: Generate README.md content.
*   **`write_readme`**: Write README.md to file.
*   **`write_docs_index`**: Write docs/index.md to file.

**Dependencies**
----------------

*   **`logging`**: For logging verbosity control.
*   **`sys`**: For exit handling.
*   **`pathlib`**: For path resolution.
*   **`src.cli`**: For command-line argument parsing.
*   **`src.llm`**: For Ollama and model verification.
*   **`src.scanner`**: For source file scanning and tree building.
*   **`src.utils`**: For logging and progress bar utilities.
*   **`src.writer`**: For writing documentation files.

**Usage Example**
-----------------

```python
python main.py --root /path/to/project --output /path/to/docs --model model_name
```

This will generate documentation for the project at `/path/to/project` and write it to `/path/to/docs`. The `--model` flag specifies the model to use for generating documentation.